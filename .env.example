# Leave these unset to run in demo mode (placeholder assets, no key required).
#
# Set both to connect a real model gateway. Then implement callGateway()
# in app/api/generate/route.ts to match your provider's API shape.
GENAI_GATEWAY_URL=
GENAI_API_KEY=
